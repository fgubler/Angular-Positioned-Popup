export enum LatchOrientation {
  horizontal = 'horizontal',    // the latch is to the left or to the right of the clicked coordinates
  vertical = 'vertical',  // the latch is above or below the clicked coordinates
}
